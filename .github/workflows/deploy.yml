name: Deploy to Beget

on:
  push:
    branches:
      - main

# Only one deploy at a time
concurrency:
  group: deploy-beget
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Write env
        run: |
          cat > .env.local << 'EOF'
          NEXT_PUBLIC_SITE_URL=https://binayah.ru
          NEXT_PUBLIC_RU_SITE_URL=https://binayah.ru
          NEXT_PUBLIC_API_URL=https://binayah-api.onrender.com
          API_BASE_URL=https://binayah-api.onrender.com
          NEXT_PUBLIC_WHATSAPP_NUMBER=971549988811
          NEXTAUTH_URL=https://binayah.ru
          NEXT_DEFAULT_LOCALE=ru
          EOF

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          # VPS has 960MB RAM + 2GB swap. 4096 let Node's heap balloon and the
          # kernel OOM-killed the runner (it then stayed dead for days). 1536
          # builds fine here and stays within RAM+swap.
          NODE_OPTIONS: '--max-old-space-size=1536'

      - name: Swap & reload PM2
        run: |
          set -e
          # Ensure swap exists (prevents OOM on 1GB VPS)
          if [ ! -f /swapfile ]; then
            fallocate -l 2G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile
            echo '/swapfile none swap sw 0 0' >> /etc/fstab
          fi
          # Copy build output to serve dir. Exclude .next/cache — it's the
          # webpack build cache (can be 400MB+), only used at build time, never
          # by `next start`. Shipping it to the small VPS disk filled it up and
          # crash-looped the app ("no production build in .next"). The explicit
          # rm guarantees it's gone even if an old copy lingers (rsync --exclude
          # protects excluded paths from --delete).
          mkdir -p /var/www/binayah-ru
          rsync -a --delete \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.next/cache' \
            ./ /var/www/binayah-ru/
          cd /var/www/binayah-ru
          rm -rf .next/cache
          npm install --omit=dev --ignore-scripts 2>/dev/null || true
          pm2 reload binayah-ru 2>/dev/null || pm2 start npm --name binayah-ru -- start
          pm2 save
