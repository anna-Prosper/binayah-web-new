# Agent Notes

- Repository: `anna-Prosper/binayah-web-new`.
- After making code changes in this repo, commit and push the active branch to `origin` so Vercel can deploy, unless the user explicitly says not to push.
- Keep commits scoped. Do not include unrelated local files or generated workspace state in pushes.
